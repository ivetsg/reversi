<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.reversi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:label="@string/nomJoc"
        android:theme="@style/AppTheme">

        <activity android:name=".Menu_principal" android:label="@string/nomJoc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>xit
        <activity android:name=".Ajuda" android:label="@string/ajuda"/>
        <activity android:name=".Configuracio" android:label="@string/configuracio" />
        <activity android:name=".Joc" android:label="@string/partidamarxa" />
        <activity android:name=".Resultat" android:label="@string/resultatsPartida" />
    </application>

</manifest>